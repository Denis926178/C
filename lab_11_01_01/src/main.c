#include <stdio.h>
#include <stdlib.h>
#include "my_snprintf.h"

int main(void)
{   
    // char buffer[110];
    // char buffer_2[110];
    
    // //char c = 'a';
    // short int d = -9307;
    // //char *s = "abddc";

    // int j = my_snprintf(buffer, 0, " ");
    // int k = snprintf(buffer_2, 0, " ");

    // printf("%s %d\n", buffer, j);
    // printf("%s %d\n", buffer_2, k);

    // j = my_snprintf(buffer, 20, "%ld", (long)d);
    // k = snprintf(buffer_2, 20, "%ld", (long)d);

    // printf("%s %d\n", buffer, j);
    // printf("%s %d\n", buffer_2, k);

    // j = my_snprintf(buffer, 20, "%hd", d);
    // k = snprintf(buffer_2, 20, "%hi", d);

    // printf("%s %d\n", buffer, j);
    // printf("%s %d\n", buffer_2, k);  

    return EXIT_SUCCESS;
}